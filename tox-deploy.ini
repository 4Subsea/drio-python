[tox]
envlist = py38
isolated_build = True

[testenv]
deps =
    build
    sphinx>=1.6.1
    sphinx_bootstrap_theme

commands =
    python -m build --outdir ./build
    sphinx-build -b html ./docs ./build/html
