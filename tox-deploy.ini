[tox]
envlist = py39
isolated_build = True

[testenv]
deps =
    build
    sphinx==5.3.0
    pydata_sphinx_theme==0.11.0

commands =
    python -m build --outdir ./build
    sphinx-build -b html ./docs ./build/html
