[tox]
envlist = py38
isolated_build = True

[testenv]
deps =
    build
    sphinx
    pydata_sphinx_theme

commands =
    python -m build --outdir ./build
    sphinx-build -b html ./docs ./build/html
